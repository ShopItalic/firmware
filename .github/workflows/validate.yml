name: Validate catalogs
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Unit tests and signature verification
        run: python3 -m unittest discover -s tests -v
      - name: Verify every catalog entry
        run: python3 tools/catalog.py
      - name: Device index matches catalog
        run: python3 tools/device_index.py --check
